import os
from pathlib import Path

def generate_dir_report(path,
                        report_file_path,
                        show_files,
                        num_files,
                        file_size,
                        hl):
    pass



    
    
    
def generate_dir_report(path, report_file_path):    
    for root, dirs, files in sorted(os.walk(path)):
        with open(os.getcwd().__add__(r"\out.txt")) as file:
            file.write(root)


generate_dir_report(os.getcwd(), None)